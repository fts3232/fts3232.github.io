<view>
    <van-tabbar active="{{ active }}" bind:change="onChange">
        <van-tabbar-item wx:for="{{list}}" wx:key="key" wx:for-index="index" name="{{index}}" icon="{{item.icon}}" data-path="{{item.path}}">{{item.text}}</van-tabbar-item>
    </van-tabbar>
</view>